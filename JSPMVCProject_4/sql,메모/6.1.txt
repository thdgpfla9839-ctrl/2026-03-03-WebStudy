6.1

마이바티스는 컬럼명과 반드시 일치
오늘은 마이바티스 이용
-------------------------------------------------------------------------------------

결과값 출력(=요청) => jsp가 담당 => view에서 결과값 읽는다
     |
요청 받기 / model 찾기 =>controller
    |
결과값 전송
    |
요청 처리
비지니스 로직(=데이터 처리)

---------------------------------------------------------------------------------------
model의 역할은 밑에 여러가지를 처리한다
1) vo
2) dao
3) 통합 request 값 담기
    model
4) open api / websocket

--------------------------------------------------------------------------------------
sql 문장에서 
OFFSET ? ROWS FETCH NEXT 10 ROWS ONLY
?에 값을 넣어주라는 코드는
=> ?대신 #{ start } 이렇게 변경해줘야 한다
------------------------------------------------------------------------------------
폴더를 주고 들어가면? 뭐가 좋다는데 다시 체크하기
폴더 단위로 키를 주자?