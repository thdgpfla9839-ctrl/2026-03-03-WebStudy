프로젝트 만들기 전에 빈jsp파일 한번 실행하기 그래야 서버.xml에 실행된다
후에 server.xml <context> 부분 추가하기 - refresh

project - clean 에러가 없는 상태에서 에러가 뜨면 프로젝트 클린
커넥션 풀이 안 되면 톰캣클린

list.jsp 파일
<c:set var="count" value="${count }"/>
         <c:forEach var="vo" items="${list }">
           <tr>
             <td width=10% class="text-center">${count }</td> 여기부분이 vo.no에서 count로 변경시킴
             <td width=45%>${vo.subject }</td>
             <td width=15% class="text-center">${vo.name }</td>
             <td width=20% class="text-center">${vo.dbday }</td>
             <td width=10% class="text-center">${vo.hit }</td>
          </tr>
           <c:set var="count" value="${count-1 }"/>
         </c:forEach>

=> count로 변경해줘야 새글에서 번호목록이 순차적으로 정렬이 된다


화면출력은 JSTL과 EL을 이용
태그 이동은 <a>를 이용한다
